# DAY 4 할일 — 이벤트 기반 블랙박스 로깅 (계획 단계, 아직 미구현)

> DAY3에서 나눈 대화에서 출발한 계획: "지금 이렇게 데이터가 계속 쌓이는데,
> 이걸 DB에 다 저장하면 낭비 아닌가?" → "평소엔 저장 안 하고 의미 있는 순간만 저장하자."
> 아직 코드는 없고, 뭘 만들지 정리만 해둔 상태.

> 관련 문서: [REMOTE_STREAMING.md](./REMOTE_STREAMING.md) — "같은 공유기가 아니어도 센서 데이터를 보려면?"
> (ESP32 직접 WiFi / MQTT / 터널 / 릴레이 선택지 정리, 이것도 미구현)

---

## 1. 왜 필요한가 — 실제로 "기록을 남겨서 나중에 보는" 사례들

- **드론/로봇 비행 로그**: 추락하거나 이상 동작하면 그 순간 자세가 어떻게 틀어졌는지 사후 분석
- **차량 블랙박스 / 보험 텔레매틱스**: 급제동·급회전·사고 순간의 IMU로 사고 재구성, 운전 습관 평가
- **낙상 감지 웨어러블**: 급격한 가속도/자이로 변화 = 낙상 이벤트로 판단, 그 앞뒤 구간만 저장해 의료진이 확인
- **스포츠 동작 분석 / 모션 캡처**: 스윙·투구 동작을 기록했다가 리플레이하며 분석
- **설비 예지보전**: 진동 패턴 추세를 장기간 기록해서 고장 전에 이상 징후 포착

공통점: **평소엔 저장 안 하고, "의미 있는 순간"만 저장한다.** 매초 50줄씩 다 저장하면
하루에 수백만 행이 쌓여서 낭비가 크다 (DAY3에서 계산해본 내용).

---

## 2. 설계 방향 — 링 버퍼 + 이벤트 트리거

```
[메모리 링 버퍼] 최근 N초치 (roll,pitch,yaw)만 계속 덮어쓰며 보관 — 디스크에 안 씀
        │
        │ 프레임 간 각도 변화량이 임계값을 넘음 (= 급격한 움직임 감지, "트리거")
        ▼
[트리거 시점 앞뒤 몇 초]만 통째로 SQLite 같은 DB에 저장
```

- **트리거 조건(안)**: 한 샘플과 다음 샘플 사이 `|roll 변화| + |pitch 변화|`가 임계값(예: 6°)을 넘으면 이벤트로 판단
- **저장 범위**: 트리거 **이전** N초(링 버퍼에 이미 있던 것) + 트리거 **이후** M초를 한 이벤트로 묶어서 저장
- **이벤트 병합**: 기록 도중에 또 트리거가 걸리면 종료 시점을 뒤로 늘려서, 연속 동작을 이벤트 하나로 합침 (잘게 쪼개지는 것 방지)
- **DB 스키마(안)**:
  - `events` — 이벤트 1건당 1행 (`id`, `triggered_at`, `peak_delta`, `sample_count`)
  - `readings` — 이벤트에 속한 개별 샘플들 (`event_id`, `offset_ms`, `roll`, `pitch`, `yaw`)
- 이 방식이면 가만히 두는 시간이 대부분인 평소엔 DB가 거의 안 자라고, "움직인 순간"만 쌓임

## 3. 어디에 붙일까

DAY3 구조에서 `roll,pitch,yaw`를 매 줄 파싱하는 곳이 `DAY3_imu_dashboard/bridge/serial_to_ws.py`의
`read_serial_loop`이므로, 거기에 이벤트 로거 모듈을 하나 더 붙이는 게 가장 자연스럽다
(ESP32 펌웨어나 대시보드는 안 건드려도 됨).

---

## 4. 해야 할 일 (TODO)

- [ ] 이벤트 로거 모듈 작성 (링 버퍼 + 트리거 감지 + SQLite 저장)
- [ ] `DAY3_imu_dashboard/bridge/serial_to_ws.py`에서 매 샘플마다 로거 호출하도록 연결
- [ ] 저장된 이벤트 목록/샘플을 조회하는 간단한 스크립트 (예: `list_events.py`)
- [ ] 트리거 임계값(`6°` 등)이 적절한지 실제 보드로 테스트하며 튜닝
- [ ] 종료 시(Ctrl+C) 기록 중이던 이벤트가 있으면 마저 저장하고 끝내기
- [ ] (선택) 대시보드에 "최근 이벤트 목록" 패널 추가해서 클릭하면 그 구간 리플레이
- [ ] 다 만들고 나면 이 폴더 이름/README를 실제 구현 내용에 맞게 다시 정리, 루트 README 학습 기록 표에 추가
